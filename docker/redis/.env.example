# Separate Redis deployment for DouyinMall.
# Use a strong password and only allow the application hosts in your firewall/security group.

REDIS_PASSWORD=REPLACE_WITH_LONG_RANDOM_SECRET
REDIS_PUBLISH_PORT=16379
REDIS_BIND_ADDRESS=0.0.0.0
