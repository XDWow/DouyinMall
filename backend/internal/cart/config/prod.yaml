db:
  host: "134.175.67.79"
  port: 3306
  user: "app"
  password: ""
  database: "douyin_mall"
  params: "charset=utf8mb4&parseTime=True&loc=Local"

redis:
  addr: "106.52.154.235:6379"
  password: ""

etcd:
  endpoints:
    - "etcd:2379"

grpc:
  server:
    name: cart.service
    port: 8099
    etcdTTL: 60

http:
  server:
    port: 18099

rocketmq:
  endpoint: "43.136.121.171:8081"
  access_key: ""
  secret_key: ""
  consumer_group: "cart-order-consumer"
  await_duration_sec: 5
  max_message_num: 16
  invisible_duration_sec: 30

mcp:
  server:
    addr: ":19094"
    name: "cart-mcp"
    version: "1.0.0"
  upstream:
    service_name: "cart.service"
    direct_addr: "127.0.0.1:8099"
  tools:
    - key: "add_to_cart"
      name: "add_to_cart"
      description: "Add a product into the current user's cart."
      enabled: true
