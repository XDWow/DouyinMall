// Package domain：智能客服领域模型与编排类型。
//
// 模型：State 是节点间共享的「公共上下文」（会话、意图、检索等，可读写）。
// TurnInput 是本轮用户入参（只读语义）。各节点除读写 *State 外，应用专用 XXXInput（及必要时的返回值）表达数据流；
// 不把「整份 State」当作节点的业务输出——写入 State 后下游直接读同一指针即可，无需再包一层「给下一节点的输出结构」。
package domain
