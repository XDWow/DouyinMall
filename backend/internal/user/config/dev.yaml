db:
  dsn: "root:root@tcp(localhost:13316)/DouyinMall_user"

redis:
  addr: "localhost:6379"

etcd:
  endpoints:
    - "localhost:12379"

grpc:
  server:
    name: user.service
    port: 8091
    etcdTTL: 60