db:
  dsn: "root:root@tcp(localhost:13306)/DouyinMall"

redis:
  addr: "localhost:6379"

etcd:
  endpoints:
    - "localhost:12379"

grpc:
  server:
    name: inventory.service
    port: 8094

kafka:
  brokers:
    - "localhost:9092"
