    // mu, _ := r.Locks.LoadOrStore(key, &sync.Mutex{})
    // lock := mu.(*sync.Mutex)
    // 使用TryLock实现带超时的锁获取，防御性编程，正常不会死锁
    // start := time.Now()
    // for {
    //  if lock.TryLock() {
    //      defer lock.Unlock() // 确保最终解锁
    //      break
    //  }
    //  if time.Since(start) > 10*time.Second {
    //      return nil, fmt.Errorf("系统繁忙，请稍后重试")
    //  }
    //  time.Sleep(100 * time.Millisecond)
    // }

    // channel，不用忙等，直接阻塞等待
    // 但是不像 Mutex 是“专用锁原语”，专门为锁优化过的结构
    // 而 channel 是“通用通信机制”，要检查很多，挂起，上下文切换，性能差，超时控制可以用，其他不要滥用
    // lockChan := make(chan struct{}, 1)
    // select {
    // case lockChan <- struct{}{}:
    //  defer func() { <-lockChan }()
    // case <-time.After(10 * time.Second):
    //  return nil, fmt.Errorf("系统繁忙，请稍后重试")
    // }

    // 官方推荐 singleflight：用 Mutex 管理状态、用 WaitGroup 挂起等待结果
    // 它解决的是“合并并发请求”，不是“互斥访问”
    // 双重检查：可能其他请求已经刷新了缓存